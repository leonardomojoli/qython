fastapi
uvicorn
sqlalchemy
asyncpg  # driver async, usado pela aplicacao
psycopg2-binary  # driver SINCRONO, exigido pelas migrations do alembic.
# Nenhum `import psycopg2` aparece no codigo: o SQLAlchemy carrega o driver
# pelo esquema da URL, entao a falta so aparece em runtime, no upgrade.
pydantic[email]
email-validator
python-multipart
google-genai==2.8.0
tiktoken  # Token counting for context window management
huggingface_hub<1.0  # pin under 1.0 — transformers (transitive via presidio path) requires <1.0

# Manipulação de Arquivos e Mídia
PyMuPDF
python-pptx
moviepy==1.0.3
pytesseract
python-magic
opencv-python
fpdf2
WeasyPrint
Markdown
faster-whisper  # transcrição local self-host (CTranslate2 INT8, large-v3-turbo) — whisper.service

# RAG v2 (busca na biblioteca: dense e5 + BM25 + RRF + cross-encoder rerank)
rank-bm25  # BM25 esparso (import rank_bm25) p/ recuperação híbrida — vector_db_service
# embedder.service usa sentence-transformers (e5-base + reranker mmarco), que já está no venv;
# os modelos baixam no 1º start. Sem EMBED_SERVER_URL, o RAG cai no MiniLM local (v1).

# Segurança e Autenticação
python-jose
passlib==1.7.4
bcrypt==4.0.1
firebase-admin
cryptography  # Fernet field-level encryption (encryption_service.py)

# Privacy / LGPD compliance
presidio-analyzer  # PII detection (multi-country) — local, no SaaS
presidio-anonymizer  # Inline redaction with consistent placeholders
# spaCy model PT-BR must be downloaded after install:
#   python -m spacy download pt_core_news_lg

# Utilitários e Outros
python-dotenv
fastapi-mail
slowapi
aiofiles
aiohttp  # Async HTTP client for reference validation
openai
resend
demjson3
json-repair  # Repara JSON malformado de LLMs (vírgula faltando, truncamento, etc.)
apscheduler
psutil  # Server metrics monitoring

# Integração Google e Processamento de Arquivos Adicional
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
Pillow
qrcode[pil]
beautifulsoup4
defusedxml

# --- Dependencias diretas que faltavam neste arquivo ---
# Importadas pelo backend mas nunca declaradas aqui: em producao foram
# instaladas no venv a mao, entao o servidor rodava e qualquer clone quebrava.
# As abaixo sao importadas no TOPO dos modulos — sem elas o boot falha.
alembic  # CLI das migrations; o compose roda `alembic upgrade head` no start
chromadb  # cliente do banco vetorial
firebase-admin  # verificacao do token de login
langchain-text-splitters  # chunking dos documentos da biblioteca
numpy
requests
starlette  # importado direto, nao so como transitiva do fastapi
stripe
Werkzeug

# Redacao de PII antes de toda chamada a LLM externo (LGPD). Sem presidio o
# codigo NAO deixa de redigir — cai num detector por regex, mais fraco. Numa
# instalacao que toque dado de paciente real, mantenha estes instalados e baixe
# o modelo: python -m spacy download pt_core_news_lg
presidio-analyzer
presidio-anonymizer

# Opcionais: importados dentro de funcao, entao a ausencia degrada o recurso
# sem derrubar a aplicacao. Descomente conforme o uso.
# python-docx   # ingestao de .docx e .pptx na biblioteca
# pandas        # relatorios do servico de preferencias
# json-repair   # conserto de JSON malformado vindo do modelo
# torch         # apenas o embed_server (perfil "ml" do compose)
